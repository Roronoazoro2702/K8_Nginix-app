# Malformed values.yaml with syntax errors
replicaCount 2  # Missing colon

image:
  repository: nginx
  tag: "latest
  # Unclosed quote
  pullPolicy: Always

service
  # Missing colon
  type: ClusterIP
  port: 80
   # Incorrect indentation
   targetPort: 8080

# Invalid nesting and quotes
resources:
  limits:
    cpu: "500m'  # Mismatched quotes
    memory: 512Mi
  requests:
    cpu: 250m
   memory: 256Mi  # Wrong indentation

# Unbalanced bracket
securityContext: {
  runAsUser: 1000,
  runAsGroup: 1000

# Invalid YAML anchors
commonLabels: &labels
  app: my-app
  environment: development

deployment:
  labels: *missing_reference  # Reference to non-existent anchor