apiVersion: v1
kind: Secret
metadata:
  name: {{ include "insecure-app.fullname" . }}
  labels:
    {{- include "insecure-app.labels" . | nindent 4 }}
# Security Issue: Using Opaque secrets instead of more secure alternatives
type: Opaque
# Security Issue: Storing secrets as plaintext in the template
# instead of using a secrets management solution
stringData:
  # Security Issue: Hardcoded credentials
  database-username: {{ .Values.databaseConfig.username }}
  database-password: {{ .Values.databaseConfig.password }}
  api-key: "sk_test_51MvGDnC9TLMNXaIZr000RdwgkOB9wMv"
  jwt-secret: "very-long-random-secret-that-should-not-be-in-the-repository"
  # Security Issue: Using weak credentials
  admin-password: "admin123"